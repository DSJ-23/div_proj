import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  helloThereStyle: {
    fontStyle: 'oblique',
    textAlign: 'center',
    color: 'red'
  }
})

export default useStyles;